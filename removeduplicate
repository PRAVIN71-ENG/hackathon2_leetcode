int removeDuplicates(int* n, int ns) 
{
    int k=1;
        for (int i=1;i<ns;i++)
        {
            if (n[i]!=n[i-1])
            {
                n[k]=n[i]; 
                k++;
            }
        }
        return k;
    
}
